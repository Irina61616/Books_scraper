# Books_scraper
This Python script is automatically collects information from multiple pages and saves the results in both JSON and CSV formats.  The scraper uses the requests library to send HTTP requests and retrieve page content, and BeautifulSoup to parse and extract relevant data from HTML.
